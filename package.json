{
  "name": "node-firebird-drivers",
  "description": "node-firebird-drivers",
  "private": true,
  "author": "Adriano dos Santos Fernandes",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "yarn workspaces foreach run build && yarn workspaces foreach run gyp:configure && yarn workspaces foreach run gyp:build",
    "clean": "yarn workspaces foreach run clean",
    "publish": "yarn workspaces foreach run publish",
    "test": "yarn workspaces foreach run test"
  }
}
